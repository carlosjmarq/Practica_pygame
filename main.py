import pygame

pygame.init()
screen = pygame.display.set_mode((800,400))
pygame.display.set_caption('DragoRunner')
clock = pygame.time.Clock()

while True:
	#draw all elements
	#update everything
	pygame.display.update()